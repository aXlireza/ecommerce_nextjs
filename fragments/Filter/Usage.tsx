import Heading from "@/components/Heading"

type Props = {

}
export default function Usage({  }: Props) {
  return (<section className={`divide-y samen-border bg-white`}>
    <Heading text={`جستجوی سریع`} underline={'partial'} />
  </section>)
}
